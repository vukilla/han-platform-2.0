# Minimal dependencies for running the Celery worker on a separate machine (for example a Windows GPU PC).
# This intentionally excludes web-server-only deps (FastAPI, Uvicorn) to reduce install surface and avoid
# dependency conflicts inside Isaac Sim / Isaac Lab Python environments.

sqlalchemy==2.0.36
numpy==1.26.4
psycopg[binary]==3.2.3
pydantic==2.9.2
pydantic-settings==2.5.2
boto3==1.35.36
celery==5.4.0
redis==5.1.1
tenacity==9.0.0
PyJWT==2.9.0
